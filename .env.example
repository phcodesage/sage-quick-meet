# Backend/WebSocket URL Configuration
# ==========================================
# For LOCAL DEVELOPMENT, use:
# VITE_WS_URL=ws://localhost:3001

# For PRODUCTION DEPLOYMENT (Vercel), use:
VITE_WS_URL=wss://sage-quick-meet.onrender.com

# HTTP API URL (optional - if not set, will be derived from VITE_WS_URL)
# For local: VITE_API_URL=http://localhost:3001
# For production: VITE_API_URL=https://sage-quick-meet.onrender.com

# Cloudflare TURN Server Configuration
# (Add your TURN server credentials here if needed)

